@model Products
@{
    ViewBag.Title = ViewBag.Action + " Product";    
}

@section scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/
                       jquery.validate.unobtrusive.min.js"></script>
}

<h1>@ViewBag.Title</h1>
<h1>@ViewBag.Title</h1>

<form asp-action="Update" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="mb-3">
        <label asp-for="CategoryID" class="form-label">Category</label>
        <select asp-for="CategoryID"
                asp-items="@(new SelectList(ViewBag.Categories,
                         "CategoryID", "CategoryName",
                         Model.CategoryID.ToString()))"
                class="form-select"></select>
    </div>

    <div class="mb-3">
        <label asp-for="ProductBrand" class="form-label"></label>
        <input asp-for="ProductBrand" class="form-control">
    </div>

    <div class="mb-3">
        <label asp-for="ProductModel" class="form-label"></label>
        <input asp-for="ProductModel" class="form-control">
    </div>

    <div class="mb-3">
        <label asp-for="ProductPrice" class="form-label"></label>
        <input asp-for="ProductPrice" class="form-control">
    </div>

    <input type="hidden" asp-for="ProductID" />

    <button type="submit" class="btn btn-primary">@ViewBag.Action</button>
    <a asp-action="List" class="btn btn-primary">Cancel</a>
</form>